export enum Routes {
  // Home stack
  Home = 'HOME',

  // Profile stack
  Profile = 'PROFILE',
}

export enum Tabs {
  Home = 'HOME_TAB',
  Profile = 'PROFILE_TAB',
}

export enum Stacks {
  Home = 'HOME_STACK',
}
