import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  line: {
    width: 5,
    height: 40,
    backgroundColor: '#b30000',
    marginRight: 10,
  },
});
