import { Pressable } from 'react-native';
import styled from 'styled-components';

export const ExpandedItem = styled(Pressable)`
  justify-content: flex-start;
  /* height: 80px; */
  padding-left: 15px;
  padding-right: 30px;
  overflow: hidden;
`;
